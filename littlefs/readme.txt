This is a readme.txt file
When you install the software on you ESP using the Web instaler 
It will also build the LittleFs from the LittleFs folder.
And upload this to the spiffs/LittleFs partition.  
You can delete the index.html and the editor.html as they are also located in the Flash Program So you do not need to have them in LittleFs.
But if you whant to change them you can do this in the LittleFs.